/* eslint-disable @typescript-eslint/no-unused-vars */
import assert from 'assert';
import { FooAdapter } from '../src';

describe('FooAdapter', () => {
  it('FooAdapter', async () => {
    const fooAdapter = new FooAdapter();
    assert.equal(true, true);
  });
});
